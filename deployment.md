# 部署说明

当前项目是本地文档和配置骨架，不包含自动交易能力。

## 本地运行边界

- 不连接券商交易接口。
- 不登录银行、支付或资金账户。
- 不保存密码、验证码、交易口令。
- 不自动买入、卖出、申购、赎回。
- 所有交易由本人手动确认和执行。

## 后续可接入组件

- 行情抓取：只读行情和公告数据。
- 定时任务：生成报告和提醒。
- 微信/IM 推送：发送风险和计划提醒。
- 本地日志：记录交易、复盘和操作草稿。

## 环境建议

1. 使用 Git 管理文档变更。
2. 将敏感配置放在未纳入版本管理的本地文件中。
3. `config/config.example.yaml` 只放非敏感样例。
4. `reports/` 只保存报告和提醒草稿。

## 试运行

先按 `docs/trial_run.md` 运行 3-5 个交易日。确认提醒频率、报告质量和规则边界后，再考虑增加盘中扫描或 IM 推送。

